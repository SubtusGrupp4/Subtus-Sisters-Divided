using System.Collections; 
using System.Collections.Generic; 
using UnityEngine; 

public class Checkpoint : MonoBehaviour 
{
    /*
    public override void PushCheckpointManager()
    {
        SafepointManager.instance.Checkpoint(transform, child.transform);
    }
    */
}
